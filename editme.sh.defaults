# Edit these!
# Your domain which is managed through porkbun (example: woofmeow.example)
domain="<domain>"
# Your SSH key ID from Digital Ocean (take from `doctl compute ssh-key list`, e.g. 123456789
ssh_key="<ssh_key>"
# Your email address, can be used in Ansible Playbooks (used in XSS Hunter for Let's Encrypt)
mail="<email>"
# TTL for subdomain
ttl=300
